---

## 🧪 Optional: Running a Quick Pipeline Test

If you do not have raw data or massive reference databases downloaded yet, you can run our built-in structural framework mock test environment to verify your system paths:

```bash
# 1. Generate the small mock test files and database placeholders
bash generate_mock_data.sh

# 2. Run the master pipeline script to verify tool pathways
bash pipeline.sh
```
